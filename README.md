# Mythoskolis Starter (Astro + Tailwind v4 + Decap CMS)

## Scripts
- `npm install`
- `npm run dev`
- `npm run build`

## Netlify
- `netlify.toml` already set (`publish = dist`).
- Admin at `/admin` after deploy, with Netlify Identity + Git Gateway.
